import { MintEvent } from '@zoralabs/zdk/dist/queries/queries-sdk';
import { TokenTransferEvent, TransferEvent } from '../../../types';
import { TransformFunctionParam } from './transformMarketEvent';
export declare function transformMintEvent({ event, at, price, eventType, }: TransformFunctionParam<MintEvent, TokenTransferEvent['eventType']>): TransferEvent | undefined;
